# 橘记 - 小程序当前现状与后续方向

> 最后更新：2026-05-15
> 本文档已从早期“头脑风暴稿”更新为“当前项目现状文档”。
> 目的不是继续空谈功能，而是给后续开发、联调、UI 重构、AI 协作提供可靠上下文。
> 文档中会明确区分：已实现、半实现、未实现、已知问题。

---

## 一、产品定位

**一句话：** 一个偏轻量、偏高频、偏低心理负担的手动记账微信小程序。

当前项目仍然坚持以下定位：

- 不是理财平台
- 不是自动记账工具
- 不是复杂财务分析系统
- 是一个“打开就能记一笔，月底能看清钱花去哪”的小程序

当前更适合的目标用户仍然是：

- 大学生
- 初入职场用户
- 对复杂记账 App 有畏难情绪的人

---

## 二、当前整体判断

从代码实际状态看，项目已经不再是纯概念阶段，而是进入了**可运行的 MVP 阶段**。

当前整体判断如下：

- 最成熟的部分：登录页、记账页、`custom tabBar`
- 基本可用的部分：首页、预算页、我的页
- 相对薄弱的部分：引导页、统计页
- 已具备主流程：登录 -> 引导 -> 首页/记账/统计/预算/我的
- 已具备基础数据闭环：用户资料、账单、预算、账单导出
- 还不适合定义为“完整上线版”，因为视觉统一、引导方式、服务端收口、主题系统都还没真正完成

一句话总结当前版本：

**主流程已经有了，UI 也开始形成风格，但工程收口和高级体验还没有做完。**

---

## 三、当前页面与导航结构

`app.json` 当前注册页面如下：

```json
[
  "pages/login/login",
  "pages/guide/guide",
  "pages/home/home",
  "pages/stats/stats",
  "pages/record/record",
  "pages/budget/budget",
  "pages/profile/profile"
]
```

当前底部导航已启用 `custom tabBar`，属于已经落地的方案，不再是设想。

底栏实际结构为：

- 首页
- 统计
- 中间主按钮：记一笔
- 预算
- 我的

当前底栏风格特点：

- 中间“记一笔”是视觉主按钮
- 其余 4 个入口已换成较柔和的 SVG 风格图标
- `app.json` 里仍保留旧的 PNG `iconPath` 配置，这是微信小程序配置层面的兼容需要，不代表业务 UI 还在用旧图标

---

## 四、各页面真实现状

### 1. 登录页

当前登录页已经不是模板页，而是项目里完成度较高的页面之一。

已实现状态：

- 有品牌化登录视觉
- 背景已采用柔和漂浮图标的设计思路
- 主按钮是“微信一键登录”
- 辅助文案已调整为“无需输入密码，直接一键体验”
- 点击按钮后会先执行静默登录，再判断是否看过引导页

当前逻辑：

- 未看过引导页：跳转到引导页
- 已看过引导页：直接进入首页

说明：

- 登录页的设计语言已经开始成为整个项目的视觉基准
- 后续其他页面的图标、颜色、质感，建议继续向登录页靠齐

### 2. 引导页

当前引导页是**4 页 swiper 版本**，并不是后续想做的“蒙层高亮 / 聚光灯引导”。

已实现状态：

- 首次进入时展示
- 看完后通过本地 `Storage` 的 `has_seen_guide` 标记避免重复出现
- 看完后进入首页

当前不足：

- 仍然是传统轮播式引导
- 不是强引导
- 不具备高亮按钮、聚光灯、手势提示等能力
- 视觉完成度和当前登录页、记账页相比偏弱

结论：

- 当前引导页是“能用版”
- 未来应重做为你想要的 Spotlight / Overlay / Coach Marks 方案

### 3. 首页

首页当前已经具备基础概览能力。

已实现状态：

- 展示本月支出
- 展示本月收入
- 展示当月日期进度
- 展示最近 10 笔账单

当前实现特点：

- 数据来自 `bills` 集合
- 首页列表里的分类图标目前仍主要使用 emoji 映射
- 首页更偏“数据入口页”，视觉上还不是最终精修状态

当前不足：

- 图标风格与登录页、记账页的柔和 SVG 语言仍不统一
- 信息结构是够用的，但还不够“高级感”
- 如果后面要做整站 UI 提升，首页会是重点页面之一

### 4. 记账页

记账页是当前业务能力最完整、UI 也相对最成熟的页面。

已实现状态：

- 支出 / 收入切换
- 金额输入
- 分类选择
- 备注输入
- 照片上传
- 保存账单
- 保存前前端校验
- 保存频率限制
- 日提交上限限制

当前 UI 特点：

- 已经有明显的卡片化布局
- 金额区、分类区、备注区、照片区已经分层
- 保存按钮悬浮在底栏上方
- 收入和支出使用不同分类集
- 分类图标开始向登录页的柔和图标风格靠拢

当前不足：

- `pickDate()` 目前还是占位逻辑，只弹提示，没有真正日期选择流程
- “新增自定义分类”的入口已预留，但分类管理页还没做出来
- 当前保存时是前端直接写 `bills` 集合，并没有真正走 `cloudfunctions/bills` 这条服务端校验链路

这个点很重要：

- **UI 很成熟**
- **交互基本能用**
- **但工程上还没有彻底收口**

### 5. 统计页

统计页当前已经有基础数据汇总，但还远未达到最终目标。

已实现状态：

- 支持按月份查看
- 会统计当前月份的支出分类
- 会生成分类金额及占比列表
- 会显示一条评论文案

当前真实情况：

- 评论文案来自本地 `COMMENTS` 数组随机选择
- 不是 AI 生成
- 统计主要是“分类列表 + 百分比”这种轻量展示
- 当前代码没有体现出真正复杂图表系统接入

当前不足：

- 和最初设想的“AI 俏皮评论”不一致
- 和最初设想的“周/月多维趋势分析”不一致
- 当前更接近“基础统计页”，不是“亮点页”

结论：

- 统计页目前能看
- 但还不够有趣，也不够强
- 未来应该继续做成产品亮点页，而不是仅仅当作信息展示页

### 6. 预算页

预算页已经具备“设预算 + 看本月消耗”的核心能力。

已实现状态：

- 设置月度预算
- 从 `budgets` 集合读取当前预算
- 统计当月支出
- 计算预算使用百分比
- 根据状态输出文案提示

当前特点：

- 核心逻辑已经有了
- 文案反馈也已有一定情绪化表达

当前不足：

- 可视化表现仍偏基础
- 预算页目前更偏“功能已通”，不是“体验精修完成”
- 若页面里存在环形进度类视觉，其 JS 侧未看到完整绘制逻辑支撑

### 7. 我的页

“我的”页面已经具备账户资料和设置的雏形。

已实现状态：

- 读取 `users` 集合中的用户资料
- 展示头像和昵称
- 性别设置
- 主题选择
- 数据导出
- 清除数据入口

当前真实情况：

- 性别设置已可写回数据库
- 主题选择已可写回数据库，并写入本地 `Storage`
- 数据导出已经不是纯设想，而是**本地已有实现**
- 自定义分类管理入口仍未完成，当前点击后只是提示“即将开放”

当前问题：

- `clearData()` 当前实际只删除了 `bills`，但弹窗文案写的是“删除所有账单和预算数据”，文案与行为不一致
- 主题虽然能切换字段，但整个小程序并没有真正形成全局主题系统

---

## 五、当前技术架构现状

### 1. 前端技术形态

当前是标准微信小程序结构：

- `WXML`
- `WXSS`
- `JS`
- `wx.cloud`

`app.js` 中已初始化 CloudBase，当前环境 ID 为：

```text
lajiaoyou-d4g78yts61f1a841d
```

并且已经具备：

- 小程序启动时静默登录
- 获取 `openid`
- 自动同步 `users` 集合中的用户资料

### 2. 当前数据库集合

从代码看，当前核心集合仍是这 3 个：

- `users`
- `bills`
- `budgets`

可理解为：

- `users`：用户资料、主题、性别、自定义分类等
- `bills`：每一笔收入 / 支出账单
- `budgets`：按月预算记录

### 3. 当前云函数现状

当前项目里能看到的云函数主要有：

- `quickstartFunctions`
- `bills`
- `exportBills`

真实情况分别如下：

#### `quickstartFunctions`

当前实际用途：

- 主要用于 `getOpenId`

遗留问题：

- 里面还保留了不少模板示例代码，例如 `sales` 集合相关的增删改查
- 说明这个云函数还带有明显的模板遗留痕迹

#### `bills`

当前已有能力：

- 服务端校验账单字段
- 创建账单
- 删除账单

但当前现实问题是：

- 前端记账页并没有调用它
- 目前记账页还是直接 `db.collection('bills').add()`

这意味着：

- 云函数里的服务端校验能力虽然写了
- 但主业务链路还没有真正用上

#### `exportBills`

当前已有能力：

- 查询账单
- 生成 CSV
- 上传到云存储
- 返回文件 `fileID`
- 在前端支持打开、分享

结论：

- “数据导出”已经不该再写成未来功能
- 它已经是**部分完成且基本可用**的能力

### 4. 自定义底栏现状

当前 `custom-tab-bar` 已经正式接管底部导航。

实际情况：

- 中间主按钮为“记一笔”
- 其余标签用柔和风格 SVG 图标
- 页面 `onShow` 中基本都在手动调用 `updateSelected()` 同步当前选中状态

这个模块已经是当前项目的稳定骨架之一。

---

## 六、数据校验与前端保护现状

### 1. 前端校验

`miniprogram/utils/validate.js` 当前已实现：

- 类型校验
- 金额校验
- 小数位校验
- 分类长度校验
- 日期格式校验
- 未来日期校验
- 备注长度校验

当前处理特点：

- 前端不再强校验本地照片路径
- 照片是否合法，预期应交给服务端最终把关

### 2. 前端频率限制

`miniprogram/utils/rateLimiter.js` 当前已实现：

- 3 秒内禁止重复保存账单
- 统计请求节流
- 本地日上限计数

说明：

- 这些保护已经有了
- 但仍属于前端层保护，不等于真正完整的服务端安全收口

---

## 七、当前版本能力划分

### 已实现

以下能力可以视为当前版本已经落地：

- CloudBase 初始化与静默登录
- 首次引导状态记录
- 自定义底部导航栏
- 登录页品牌化 UI
- 账单新增
- 账单照片上传到云存储
- 首页月度概览
- 统计页基础分类统计
- 月预算设置与预算消耗计算
- 用户性别设置
- 用户主题字段选择与保存
- 账单 CSV 导出

### 半实现

以下能力已经有雏形，但不能算真正完成：

- 引导页：已存在，但不是最终想要的 Spotlight 引导
- 主题系统：有主题字段，但无全局视觉切换闭环
- 自定义分类：数据库字段与入口已存在，但管理页未完成
- 统计评论：有评论文案，但目前不是 AI 评论
- 预算页视觉：有逻辑，但可视化表达仍较弱
- 服务端校验：云函数已写，但前端主流程未接入

### 未实现或明确待重做

以下内容仍应视为后续任务，不应误认为已完成：

- 蒙层高亮 / 聚光灯引导页
- 手势引导类新手教学
- 真正全局生效的主题系统
- 自定义分类完整 CRUD 管理
- AI 生成统计评论
- 更完整的周度 / 月度趋势分析
- 首页与统计页图标语言统一重构
- 记账页日期选择器完整接入

---

## 八、当前已知工程问题

这部分很关键，后续无论是谁接手，都应先看这一节。

### 1. 文案和真实行为不一致

`profile` 页的“清除数据”弹窗写的是删除账单和预算，但当前代码只删除了账单。

### 2. 服务端链路没有真正收口

虽然存在 `cloudfunctions/bills`，但当前保存账单仍是前端直写数据库。

### 3. 模板遗留仍存在

`quickstartFunctions` 里还保留了模板 `sales` 相关逻辑，说明工程还没有完全去模板化。

### 4. 视觉语言尚未全站统一

登录页和记账页已经开始形成品牌感，但首页、统计页、我的页还没有全部跟上。

### 5. 引导页能力与目标差距较大

当前引导页只是普通 swiper，和你后面要做的“聚光灯式强引导”不是一回事。

### 6. 工程里存在预览兼容性遗留物

当前 `miniprogram/components/cloudTipModal/` 更接近为兼容预览报错而保留的占位组件，不是正式业务组件。

### 7. 工作区并非绝对干净

当前本地工程还存在未提交修改，后续任何 AI 或开发者接手前，都应该先看 `git status`，避免误覆盖。

---

## 九、下一阶段建议优先级

如果以后继续推进，我建议按这个顺序做，而不是同时乱改：

1. 先把自定义分类管理页补完整，形成“创建分类 -> 记账使用 -> 首页/统计识别”的闭环
2. 把记账、删除、批量敏感操作尽量收口到云函数，减少前端直写数据库
3. 重做引导页，升级为“蒙层高亮 / 聚光灯引导”方案
4. 做全局主题系统，让“主题切换”不只是改字段，而是真正改全站颜色与组件样式
5. 重做统计页，让它成为真正的亮点页，再考虑 AI 评论接入
6. 统一首页、统计页、我的页的图标语言与页面质感，向登录页和记账页靠齐

---

## 十、后续协作时的上下文结论

如果后面继续让 AI 接手这个项目，最重要的不是再重复“我要做一个记账小程序”，而是直接告诉它以下事实：

- 这是一个已经有 MVP 的微信小程序，不是从 0 开始
- 当前最成熟的是登录页、记账页、custom tabBar
- 当前最需要补的是分类管理、引导页重做、统计页强化、主题系统闭环
- 当前统计评论不是 AI
- 当前导出能力已经有了
- 当前记账保存没有真正接入服务端校验云函数
- 当前工作区可能有未提交修改，不能贸然覆盖

这几条比旧版脑暴信息更重要，也更适合作为下一轮开发的真实上下文。
