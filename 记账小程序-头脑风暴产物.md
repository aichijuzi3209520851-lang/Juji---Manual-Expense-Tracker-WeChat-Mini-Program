# 橘记 (Juji) — 头脑风暴产物 & 工程文档

> 本文档整合了从 2026-05-09 开始的产品设计、技术决策和工程实现全过程。

---

## 一、产品定位

**一句话：** 一个让人愿意坚持用的极简手动记账习惯养成工具。

- **不是** 理财 App，**不是** 自动记账工具，**不是** 财务分析平台
- **是** 一个"打开→记账→走人"的轻量工具，核心价值是月底让用户**安心**地看到"我的钱花去哪了"

### 目标用户

- 大学生（每月生活费相对固定，消费场景简单）
- 初级职场人群（对复杂记账 App 有畏难情绪）

### 核心体验

1. **5 秒记一笔账**：打开 → 输入金额 → 选分类 → 保存，图片/备注可选，不强制
2. **数据回顾**：周度和月度消费数据清晰展示 + 俏皮搞怪的梗（非说教），让用户感到安心而非焦虑
3. **零门槛上手**：微信一键登录，不需要注册、不需要密码
4. **个性主题**：4 套预设主题 + 8 色自定义调色板 + 用户命名主题 CRUD（最多 5 个），一键切换，全 App 跟随变色

---

## 二、一笔账的数据结构

| 字段 | 类型 | 必填 | 说明 |
|:---|:---|:---|:---|
| 类型 | 支出 / 收入 | ✅ | 二选一 |
| 金额 | 数字 | ✅ | 支持小数点 |
| 分类 | 字符串 | ✅ | 预设 + 用户自创 |
| 日期 | 日期 | ✅ | 默认当天，可修改 |
| 备注 | 文本 | ❌ | 可选 |
| 照片 | 图片 | ❌ | 可选（拍照 / 从相册选择） |
| 心情 | Emoji | ❌ | 可选（8 预设 + 自定义） |

---

## 三、页面清单（10 页：5 Tab + 登录 + 引导 + 3 子页面）

### 0. 新手引导（Onboarding）
- 3 页 swiper，VisionOS 空间计算美学风格，首次安装时显示
- 弥散光背景（3 层 ambient orb，blur 80px，呼吸浮动动画）
- 毛玻璃悬浮球（backdrop-filter: blur 24px，6s 呼吸浮动）
- 巨型汉字超级符号水印（600rpx，3% 透明度暗纹）
- 错落排版文字区块 + 电影级贝塞尔曲线入场动效（cubic-bezier(0.2, 0.8, 0.2, 1)）
- 胶囊指示器（48rpx 激活态 + primary 光晕）
- 第 3 展示"开启记账之旅"毛玻璃 CTA 按钮
- 引导看过一次后不再显示（本地 Storage 标记 `has_seen_guide`）

### 1. 登录页（Login）
- 微信一键静默登录（云函数 `quickstartFunctions` → `getOpenId`）
- 精美 UI：柔光渐变背景 + 8 个 SVG 浮动图标 drift 动画
- 隐私协议入口在调试阶段临时注释，避免未提供正式协议时阻塞登录
- 登录后判断是否首次使用 → 跳转首页或引导页

### 2. 首页（Home）
- 昨天 / 今天 **收支双行卡片**（支出大字 + 收入绿色小字，双日对比）
- 当月预算三态进度条（未设引导 / 正常进度 / 超支红色）
- 季节氛围标签点缀
- 账单流水按日分组（日期 + 周几 + 当日净额），近 30 条
- 点击账单 → 手账风格详情页；长按删除（二次确认，走云函数校验）
- 网络失败重试机制

### 3. 统计页（Stats）
- 天/周/月时间跨度切换 + 收入/支出 segment toggle
- **分类排行榜**：Top 8 类目横向进度条，压缩纵向空间并强化金额对比
- **动态折线趋势图**：天/周/月独立区间（近 30 天 / 近 12 周 / 近 12 个月）+ 原生 canvas 动画绘制 + 完整横坐标刻度 + 主题色适配
- **AI 俏皮评论三卡片**「小橘的便签」：
  - 📅 昨日回顾 / 📊 上周回顾 / 📈 上月回顾
  - 横向滑动卡片（右侧露出 + 渐隐遮罩 + 分页点 + 首次滑动提示），降低纵向空间占用
  - 混元 `hunyuan-v3` → `hy3-preview` `streamText`
  - 串行调用（间隔 1s）+ 429 退避重试（2 次，3s/6s）
  - `periodKey` 自动缓存，失败不写缓存允许下次重试

### 4. 记账页（Record）— 数据唯一入口
- 收/支滑块切换（支出玫瑰色 / 收入薄荷绿双色系）
- **104rpx 大字金额输入**
- 分类网格选择（8 预设 SVG 图标 + 自创分类弹窗）
- **自创分类弹窗**：名称 input（maxlength=10）+ emoji input（maxlength=2，placeholder 提示 + JS 校验兜底），创建后自动选中
- **长按删除自定义分类**：`bindlongpress` 绑定，安全守卫（仅允许删除自定义分类，预设分类不可删），二次确认弹窗 → DB 删除 → 状态同步
- 原生日期 picker（智能显示今天/昨天/X月X日，**可选未来日期**至 2027 年）
- **心情选择器**（8 预设 emoji + 自定义输入）
- 备注文本输入
- **Neumorphic 3D 相机按钮**（100rpx 圆形，主色渐变背景 + --shadow-light 高光 + --shadow-dark 深影，按下缩小态，拍照 / 相册选择，压缩上传云存储）
- 频率限制：3s 防抖 + 单日 500 笔上限
- 输入校验（`utils/validate.js`）：金额>0、分类非空、备注≤200 字
- 新增和编辑均走云函数 `bills`（服务端校验 + 所有权检查）
- 金额键盘上方快捷确认键：可直接保存基础账单（备注空、心情默认第一个表情、图片空）
- 新增/编辑保存后自动重置表单并跳转首页，避免重复提交
- 保存后自动滚回顶部，避免连续记账时页面缓存停留在备注区或分类区
- 页面隐藏时自动清理编辑模式状态，`onUnload` 时重置表单

### 5. 预算页（Budget）
- CSS `conic-gradient` 进度环（入场 scale-in 动画）
- **消费节奏卡**：日均预算 / 今日消费 / 剩余天数 / 建议日均
- **钱去哪了**：Top 3 消费分类（emoji + 金额 + 百分比进度条）
- **6 月预算达成率历史**条形图（超支月红色标记）
- **智能建议**：基于近 3 月均值 × 90% 推荐预算额
- 居中弹窗面板调整预算额（输入 0 或留空可关闭预算功能）
- 预算保存改为 upsert（先查后改，不再产生重复记录）
- 空态引导（未设置时提示）
- 安全/警告/超支三态文案

### 6. 我的页面（Profile）
- 头像在线编辑链路：`wx.chooseMedia` → `wx.editImage`(裁剪) → `wx.compressImage`(80%) → 云存储 → `getTempFileURL`(https) → 渲染
- 昵称编辑弹窗（1-20 字校验）
- 性别设置（男/女/未设置）
- **个人资料**：出生日期 picker（自动推算星座）+ 职业/状态（5 预设选项 + 自定义输入），持久化到 `users` 集合
- **记账足迹卡**：天数 / 累计笔数 / 今日称号（AI 根据当天支出生成娱乐称号，无支出显示「待解锁」），数值区定高 60rpx + flex-end 基线对齐，标签统一 line-height: 1
- **小橘 AI 助手**：头像卡右上角圆橙子像素角色，随机招手/跳跃/发射爱心（爱心概率最高）；点击打开微信聊天式弹窗，使用混元 AI 进行轻量记账陪伴对话，弹窗展示「AI生成，仅供参考」并适配系统键盘。
- **天气询问能力已下线**：不再请求地理位置、不再依赖腾讯位置服务 Key；用户问天气时提示该功能暂时下线，避免实时天气接口配置失败影响 AI 对话。
- **AI 信件生成弹窗**（拟物书信风）：
  - 两类入口：记账天数 / 最爱分类，均调用同一信件生成逻辑
  - 云函数 `aiPoster` + 混元文本大模型 (`hunyuan-v3`, `hy3-preview`)
  - 系统 Prompt：赛博知己兼哲学家角色，150-180 字深情心里话，融合职业痛点 + 星座性格 + 消费习惯 + 日均消费
  - 传参：`days` / `category` / `zodiac`（星座）/ `occupation`（职业）/ `avgDailySpend`（日均消费）
  - 每日 30 次限制（`dailyCounters` 内存级 Map，冷启动重置）
  - `wx.showLoading` 全局 Loading + 30s timeout
  - 失败时返回兜底暖心文案（"小橘去找星星借灵感"版），用户永远看不到报错
  - **信纸弹窗**：暖纸色 `#FFFAF0` + repeating-linear-gradient 横线纹理（56rpx 行高）+ 🍊 左上角挂件（负值定位溢出）+ 称呼层「亲爱的[用户名]：」+ 正文 line-height 56rpx 对齐横线 + 右对齐落款「—— 你的赛博知己 小橘」+ 底部居中按钮
- **JSON 数据导出**：云函数 `dataMigration`(export) 查询全部账单 → 清除 `_id`/`_openid` → `wx.getFileSystemManager().writeFile` 写入本地 → `wx.shareFileMessage` 分享给文件传输助手
- **JSON 数据导入**：`wx.chooseMessageFile` 选取 `.json` 文件 → 解析清洗（删除 `_id`/`_openid`，校验核心字段）→ 云函数 `dataMigration`(import) 分批写入（每批 20 条 `Promise.all`）→ 全局 Loading「正在恢复账单数据…」
- 数据清除（二次确认弹窗，清除后足迹卡重置）

### 7. 分类管理（Categories）
- 预设分类列表（12 种，含 SVG 图标）
- 自创分类列表
- CRUD 操作（新增弹窗：名称 + emoji 图标池选择）
- 实时同步到 `users.customCategories`

### 8. 主题切换（Themes）
- **4 套预设主题 + 用户自定义主题**：
  | ID | 名称 | 描述 | 主色调 | accent（跨色相渐变） |
  |---|---|---|---|---|
  | `mint` | 清爽薄荷（默认） | 清新绿底，护眼舒适 | 薄荷绿 `#27c07d` | 蓝绿 `#5ba4cb` |
  | `fresh` | 温馨玫瑰 | 暖白底 + 柔和玫瑰色，温暖亲切 | 玫瑰棕 `#785655` | 橄榄 `#6a7855` |
  | `dark` | 夜猫子 | 深色背景 + 暖金主色，护眼夜间模式 | 暖金 `#d4a574` | 黄绿 `#a8c986` |
  | `skyBlue` | 蓝天白云 | 天蓝底 + 清透蓝色，明亮开阔 | 天蓝 `#38bdf8` | 紫 `#7e69cf` |
- **8 色自定义调色板**（Tailwind 400/500 级高饱和亮色）：樱花粉 / 蜜桃橙 / 柠檬黄 / 青葱绿 / 蒂芙尼蓝 / 鸢尾紫 / 玫瑰金 / 晴空蓝
- **用户命名主题 CRUD**：最多保存 5 个自定义主题，支持命名、切换、长按删除
- 自定义主题推导算法：accent = primary.h + 60° 色相偏移，实现跨色相渐变
- 选中 checkmark 标识 + modal 确认弹窗
- 切换流程：Storage 写入 + DB 同步 + `wx.reLaunch` 全局刷新
- 默认主题：`mint`（清爽薄荷）
- 旧版 `theme='custom'` 自动迁移到 `user_themes`

### 9. 账单详情（Detail）
- 手账便签风格
- 暖奶油渐变底
- 拍立得照片框
- 心情圆形印章
- 信纸横线纹理备注区
- 删除操作

---

## 四、主题系统架构

### 设计方案

采用 **inline-style CSS 变量注入** 方案：

```
utils/theme.js
  ├── THEME_VARIABLES = { mint, fresh, dark, skyBlue }  // 4套 × ~35个变量
  ├── generateCustomVars(hex)  // 自定义主题推导（含 accent = primary.h + 60°）
  ├── getUserThemes() / saveUserTheme() / deleteUserTheme()  // 用户主题 CRUD
  ├── migrateLegacyCustomTheme()  // 旧版 custom 主题迁移
  ├── getThemeStyleString(id) → "--color-primary:#xxx;--gradient-hero:linear-gradient(...);..."
  ├── applyTheme(id) → wx.setPageStyle(backgroundColor) + setNavigationBarColor
  └── initAppTheme(app) → 读 Storage → 写 globalData.currentTheme
```

**为什么用 inline-style？**
- `wx.setPageStyle()` API **不支持** CSS 自定义属性注入（仅支持 backgroundColor 等有限属性）
- `page{}` 中声明的 `var()` 在声明时就被解析为默认值，不会跟随子 view 的变量覆盖
- 因此每个页面最外层 view 绑定 `style="{{themeStyle}}"`，直接注入已计算的值（非 var() 引用）

**Design Token 体系（v2）：**

| 层级 | Token | 说明 |
|------|-------|------|
| 基础色 | `--color-primary` / `--color-accent` | 主色 + 跨色相互补色（60° hue 偏移） |
| 派生色 | `--color-primary-container` / `--color-primary-light` | 主色浅版（容器/高光） |
| 渐变 | `--gradient-hero` / `--gradient-hero-soft` | 主色→accent 跨色相渐变 |
| 拟物阴影 | `--shadow-light` / `--shadow-dark` | Neumorphic 高光（白）+ 深影（主题色） |
| 组合阴影 | `--shadow-soft` / `--shadow-card` / `--shadow-elevated` / `--shadow-hero` | 双侧 Neumorphic + 单侧投影 |
| 语义色 | `--color-text` / `--color-border` / `--color-surface` | 文字/边框/表面 |
| 圆角 | `--radius-sm/md/lg/xl/2xl/full` | 8~9999rpx |
| 间距 | `--space-xs/sm/md/lg/xl` | 8~40rpx |

**衍生 token 注入**：`getThemeStyleString()` 在返回基础变量后，追加计算 `--gradient-hero`、`--shadow-card` 等衍生 token 的实际值（直接写死渐变表达式），避免 CSS var() 在 `page {}` 声明时被提前解析为默认值。

**持久化：** 本地 `Storage('theme')` + `Storage('user_themes')` + `users.theme` 数据库字段（跨设备同步）

**切换方式：** themes 页面选择 → 确认弹窗 → `wx.reLaunch({url:'/pages/home/home'})` 重启生效

---

## 五、AI 能力集成

### 5.1 AI 俏皮评论（Stats 页 — 文本生成）

```
用户打开统计页
  → onShow 触发 loadAIComments()
    → 串行加载 daily → weekly → monthly（间隔 1s 防 429）
      → 检查 periodKey 缓存 → 命中则直接渲染
      → 未命中 → 构建 prompt（消费数据 + 角色设定）
        → wx.cloud.extend.AI.createModel('hunyuan-v3').streamText()
          → textStream / eventStream 双通道解析
            → cleanText() 清洗输出 → 缓存 → setData 渲染
              → 失败 → fallback "小橘暂时没词儿了"（不写缓存，允许重试）
                → 429 限流 → 自动重试 2 次（3s / 6s 退避）
```

**内容准则：**
- 调侃消费行为本身，不攻击个人
- 不给建议，不评判生活方式
- 网络热梗自然融入
- 免责声明："以上内容由腾讯混元 AI 生成，仅供娱乐参考"

### 5.2 AI 信件生成（Profile 页 — 文本生成）

```
用户点击足迹卡（天数/最爱分类）
  → wx.showLoading("小橘想对你说说心里话")
    → callFunction('aiPoster', { days, category, zodiac, occupation, avgDailySpend }, { timeout: 30000 })
      → 云函数内：检查 dailyCounters 日限额（30次/天）
        → 拼装 System Prompt（赛博知己兼哲学家，融合职业+星座+消费习惯）
        → ai.createModel('hunyuan-v3').generateText({
            model: 'hy3-preview',
            messages: [systemPrompt, userPrompt]
          })
            → result.text 获取返回文本
            → 返回 150-180 字深情心里话
              → 前端 Neumorphic 信纸弹窗展示
                → "收下心意" 按钮关闭
                  → 失败时使用兜底暖心文案（"小橘去找星星借灵感"版）
```

### 5.3 小橘 AI 助手与今日称号（Profile 页 — 文本生成）

- **今日称号**：`loadFootprint()` 汇总当天支出分类金额 → 生成消费签名 → 本地缓存命中直接展示；签名变化时调用 `aiPoster` 的 `profileTitle` action 生成娱乐称号，失败时走分类规则兜底。
- **称号解释卡片**：点击足迹卡第三项展示称号、来源分类和简短理由，标注 AI 生成娱乐属性，并保留「让小橘写封信」入口。
- **小橘 AI 助手**：Profile hero 右上角圆橙子像素角色，随机动作权重为爱心 55% / 招手 30% / 跳跃 15%；点击打开本次会话聊天弹窗。
- **聊天云函数**：`aiChat` 使用混元文本模型，前端仅传最近 6-8 条本次消息和基础用户画像；用户输入与 AI 输出均走内容安全兜底，敏感内容统一回复「小橘不知道，来聊聊别的吧~」。
- **天气能力下线**：不再请求 `wx.getLocation`，不再配置 `TENCENT_LBS_KEY`；天气类问题在前端直接提示功能暂时下线，避免第三方天气接口阻塞对话测试。

---

## 六、技术架构

### 整体方案

```
微信小程序前端（WXML + WXSS + JS）
  │
  ├─→ wx.cloud.database()   ──→ CloudBase NoSQL 数据库
  │     collections: bills, budgets, users
  │
  ├─→ wx.cloud.uploadFile() ──→ CloudBase 云存储
  │     avatars/, bills/, exports/
  │
  ├─→ wx.cloud.callFunction() → CloudBase 云函数 (Node.js)
  │     quickstartFunctions, bills, exportBills, aiPoster, aiChat, dataMigration
  │
  └─→ wx.cloud.extend.AI      → 腾讯混元大模型
        hunyuan-v3 (文本 streamText)
        hunyuan-image (图像 generateImage, v1.9)
```

### 关键选型

| 决策项 | 选择 | 理由 |
|--------|------|------|
| 后端 | CloudBase Serverless | 不买服务器，按量付费有免费额度 |
| 数据库 | CloudBase 文档型 NoSQL | 类 MongoDB，无需建表写 SQL |
| 登录 | 微信静默授权 | 零门槛，用户无感 |
| AI | 混元大模型 | 小程序成长计划送 1 亿 Token |

### 输入校验规则

| 字段 | 规则 |
|------|------|
| type | 只能是 `"expense"` 或 `"income"` |
| amount | 数字，> 0 且 ≤ 99999999.99，小数点最多 2 位 |
| category | 字符串，长度 1-20 字 |
| date | 格式 YYYY-MM-DD（允许未来日期） |
| note | 可选，≤ 200 字 |
| photoUrl | 可选，须以 `cloud://` 开头 |

### 数据库 Schema

**bills（账单）：** `_openid, type, amount, category, date, note, photoUrl, mood, createdAt`

**budgets（预算）：** `_openid, month("2026-05"), amount, createdAt`

**users（用户）：** `_openid, nickname, avatarUrl, gender, birthday, occupation, customCategories([{name,icon}]), theme, budgetDefault, createdAt, lastLoginAt`

**dailyCounters（AI信件日限）：** 云函数 `aiPoster` 内存级 Map（非数据库集合），冷启动重置，每日 30 次。

---

## 七、项目结构

```
miniprogram/                    # 小程序前端根目录
├── app.js                      # 入口：CloudBase初始化 + 主题加载 + 静默登录
├── app.json                    # 路由(10页) + TabBar配置 + 窗口样式
├── app.wxss                    # 全局 CSS 变量 Token (~42个) + 通用组件样式
├── custom-tab-bar/             # ★ 自定义毛玻璃TabBar（5列，中间凸起按钮）
│   ├── index.js                # tab切换逻辑 + 高亮同步
│   ├── index.wxml              # grid布局 + primary按钮特殊处理
│   └── index.wxss              # backdrop-filter毛玻璃 + 浮起动画
├── pages/
│   ├── login/                  # 登录页（渐变 + 8 SVG漂浮动效）
│   ├── guide/                  # 新手引导（4页swiper）
│   ├── home/                   # 首页 Tab 1
│   ├── stats/                  # 统计 Tab 2（折线趋势 + 分类排行 + AI评论）
│   ├── record/                 # 记账 Tab 3（核心入口）
│   ├── budget/                 # 预算 Tab 4（进度环 + 历史）
│   ├── profile/                # 我的 Tab 5（头像 + 海报 + 导出）
│   ├── categories/             # 分类管理子页面
│   ├── themes/                 # 主题切换子页面
│   └── detail/                 # 账单详情子页面
├── utils/
│   ├── theme.js                # ★ 多主题系统核心（4套 × ~35变量）
│   ├── eventBus.js             # 事件总线（on/off/emit，跨页面通信）
│   ├── rateLimiter.js          # 前端频率限制（3s防抖 + 500条/日上限）
│   └── validate.js             # 输入校验（账单 + 预算规则）
└── images/
    ├── login/                  # 登录页 SVG 图标（bag/utensils/plane等）
    ├── record/                 # 记账页 SVG 图标
    └── tabbar/                 # TabBar 图标

cloudfunctions/                 # 云函数 (Serverless后端)
├── quickstartFunctions/         # 静默登录(getOpenId) + 用户同步
├── bills/                       # 账单服务端校验 create/delete/update（新增和编辑均走服务端）
├── exportBills/                 # CSV导出生成器
├── aiPoster/                    # ★ AI信件生成（混元文本大模型）
└── dataMigration/               # JSON数据迁移（export查询全部 / import分批写入）
```

---

## 八、开发历程 & 已知遗留

### 已完成功能

- [x] 微信一键静默登录 + 用户资料自动同步
- [x] 10 个页面完整实现（5 Tab + 登录 + 引导 + 3 子页面）
- [x] 自定义毛玻璃 TabBar（中间记账按钮凸起）
- [x] 记账完整链路：分类/金额/日期/心情/备注/照片
- [x] 自定义分类 CRUD + emoji 图标池
- [x] 首页概览（今日/昨日 + 预算 + 分组流水）
- [x] 统计分析（天/周/月折线趋势 + 分类排行 + 收支切换）
- [x] AI 俏皮评论三卡片（混元 hunyuan-v3，带缓存和重试）
- [x] 月度预算管理（进度环 + 历史达成率 + bottom-sheet）
- [x] 头像/昵称在线编辑硬化实现
- [x] AI 信件生成（记账天数 + 最爱分类 → 俏皮鼓励信，30 次/天）
- [x] AI 信件高级重构：赛博知己 Prompt + 星座/职业/日均消费全链路传参
- [x] AI 今日消费称号（当天支出分类签名 + 缓存 + 混元生成 + 规则兜底）
- [x] 小橘 AI 助手（像素角色随机动作 + 聊天弹窗 + `aiChat` 云函数）
- [x] 小橘聊天键盘适配（隐藏自定义 TabBar + 键盘高度定位 + 防遮罩误关闭）
- [x] 天气查询链路下线（移除定位权限与腾讯位置服务依赖）
- [x] CSV 数据导出（打开/分享/保存）
- [x] JSON 数据导出/导入（dataMigration 云函数，支持环境迁移）
- [x] 自创分类 Emoji 输入校验（maxlength=2 + placeholder + JS 兜底）
- [x] 长按删除自定义分类（安全守卫 + 二次确认 + 状态同步）
- [x] 足迹卡片基线对齐（定高容器 flex-end + 统一 line-height）
- [x] AI 信件弹窗拟物书信风重构（信纸横线 + 橙子挂件 + 称呼/落款排版）
- [x] 4 套预设主题 + 8 色自定义调色板 + 用户命名主题 CRUD（最多 5 个）
- [x] Design Token 体系 v2（accent 跨色相渐变 + Neumorphic 拟物阴影）
- [x] 全局 UI 升级：小红书/Apple Wallet 风格（渐变 hero 卡 + 40rpx 大圆角 + 柔阴影）
- [x] 5 个 Tab 页 hero 渐变卡（跟随主题变色）
- [x] 记账页 Neumorphic 3D 相机按钮（主色渐变 + 高光深影）
- [x] 登录页多主题适配（CSS 变量化）
- [x] 前端频率限制 + 输入校验
- [x] 账单详情手账风格页面（token 化）
- [x] 全局硬编码颜色清除（所有 rgba(120,86,85) 替换为 token）

### 已知遗留（不影响上线）

- [x] ~~`cloudfunctions/bills` 新增(create)仍走前端直写~~ → 已改为新增和编辑均走云函数
- [x] ~~`clearData()` 仅删 `bills` 不删 `budgets`~~ → 已修复，清除后足迹卡重置
- [ ] `clearData()` 仍不删 `budgets` 集合（仅清除 bills + 重置页面状态）
- [x] ~~新手引导仍为传统 swiper，未升级到 Spotlight 高亮引导~~ → 已升级为 VisionOS 空间计算美学风格（毛玻璃悬浮球 + 汉字水印 + 弥散光背景）
- [ ] 登录页隐私协议入口当前为临时注释，等正式协议上线后需要恢复并重新验证登录链路

---

## 九、名词对照（给不熟悉的开发者）

| 名词 | 大白话解释 |
|------|-----------|
| CloudBase / 云开发 | 腾讯提供的后端服务，不用买服务器就能搞定数据库、文件存储和登录 |
| 云数据库（文档型 NoSQL） | 数据存成 JSON，读写像操作 JavaScript 对象，不需要建表、不需要写 SQL |
| 云存储 | 存文件的地方，比如用户拍的账单照片 |
| 云函数 | 跑在云端的 JavaScript 代码，处理登录、复杂计算等，不需要自己搭服务器 |
| _openid | 微信给每个用户在小程序里的唯一 ID，自动获取，用户无感 |
| Serverless | "没有服务器的后端"，你只管写代码，云平台帮你跑 |
| TabBar | 小程序底部导航栏 |
| WXML | 小程序版的 HTML（写页面结构） |
| WXSS | 小程序版的 CSS（写样式） |
| CSS 变量 / Token | 定义一次颜色值，全局通过 `var(--color-primary)` 引用，改一处全局变 |

---

> **最后更新：** 2026-06-11 · v1.0.9
> **状态：** 功能完整，待提交审核上线
