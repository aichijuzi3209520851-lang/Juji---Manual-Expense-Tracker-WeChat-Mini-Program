# 橘记 — 手动记账微信小程序 · 头脑风暴产物

> 本文档整合了从 2026-05-09 开始的头脑风暴全过程。是一份"产品+技术"参考手册，用于后续 Vibe Coding 时直接丢给 AI 作为上下文。

---

## 一、产品定位

**一句话：** 一个让人愿意坚持用的极简手动记账习惯养成工具。

- **不是** 理财 App，**不是** 自动记账工具，**不是** 财务分析平台
- **是** 一个"打开→记账→走人"的轻量工具，核心价值是月底让用户**安心**地看到"我的钱花去哪了"

### 目标用户

- 大学生（每月生活费相对固定，消费场景简单）
- 初级职场人群（对复杂记账 App 有畏难情绪）

### 核心体验

1. **5 秒记一笔账**：打开 → 输入金额 → 选分类 → 保存，图片/备注可选，不强制
2. **数据回顾**：周度和月度消费数据清晰展示 + 俏皮搞怪的梗（非说教），让用户感到安心而非焦虑。数据够多就回顾，不强制等到月底
3. **零门槛上手**：微信一键登录，不需要注册、不需要密码
4. **个性主题**：预设多套主题，一键切换

---

## 二、一笔账的数据结构

| 字段 | 类型 | 必填 | 说明 |
|:---|:---|:---|:---|
| 类型 | 支出 / 收入 | ✅ | 二选一 |
| 金额 | 数字 | ✅ | 支持小数点 |
| 分类 | 字符串 | ✅ | 预设 + 用户自创 |
| 日期 | 日期 | ✅ | 默认当天，可修改 |
| 备注 | 文本 | ❌ | 可选 |
| 照片 | 图片 | ❌ | 可选（拍照 / 从相册选择） |

---

## 三、五个页面

### 0. 新手引导（Onboarding）
- 4 页 swiper 滑动引导，首次安装时显示
- 第 1 页：随手记账
- 第 2 页：自定义分类（用户自创消费类型，从图标池自选 emoji）
- 第 3 页：周度/月度数据回顾 + 俏皮评论
- 第 4 页：个性主题切换 + 微信一键登录开始
- 引导看过一次后不再显示（本地 Storage 标记）

### 1. 首页（Home）
- 本月支出/收入概览
- 最近 5-10 笔账单列表
- 快速记账入口（浮动按钮或顶部入口）

### 2. 记账页（Add Bill）
- 数据唯一入口
- 支出/收入切换 → 金额输入 → 分类选择 → 日期 → 备注（可选）→ 照片（可选）
- 支持预设分类 + 用户自创分类（自创分类时从图标池选图标：🍜🍔🍕🍰🍿🎮📚🚌💊🛒 等 30-50 个）

### 3. 统计页（Stats）
- 月度消费饼图/柱状图（按分类），可切换查看周度或月度
- 月度/周度对比趋势
- 俏皮评论（混元 AI 生成，带免责声明）

### 4. 预算页（Budget）
- 用户手动设定月度预算金额
- 显示"本月已花 / 预算总额"进度条
- 超支/节约提示

### 5. 我的页面（Profile）
- 微信头像 & 昵称展示
- 性别设置（用于俏皮评论个性化，如"大馋小子"/"大馋丫头"）
- 自定义分类管理
- 主题切换（预设多套主题可选，每套有图标 + 名称 + 描述，如"清新""深夜""暖阳""薄荷"等）
- 数据导出（Excel，后续版本）
- 隐私设置（数据删除等）

---

## 四、主题系统设计

预设多套主题，用户在「我的」页面选择。每套主题结构：

```json
{
  "id": "fresh",
  "name": "清新",
  "description": "暖白底配柔和主色，清爽干净",
  "icon": "主题图标（用户提供）",
  "variables": {
    "--bg": "#FAFAF5",
    "--card": "#FFFFFF",
    "--text": "#1a1a1a",
    "--accent": "#FF8A80"
  }
}
```

主题选择后存本地 Storage，换手机可重置。实现方式：CSS 变量 + `page[data-theme="xxx"]` 选择器，一键切换所有页面颜色。后续可扩展用户自定义主题（取变量值存入 `users` 集合实现跨设备同步）。

---

## 五、俏皮评论方案

**当前决策：接入腾讯混元大模型（已申请 1 亿 Token，来自 AI 小程序成长计划）**

### 三条评论同时展示，刷新频率不同

统计页同时展示三条 AI 生成的俏皮评论：

| 评论类型 | 分析数据范围 | 更新频率 | 示例 |
|:---|:---|:---|:---|
| **昨日回顾** | 昨天（00:00-23:59） | 每天更新一次 | "昨天餐饮花了 68 块，占总支出的 54%，鉴定为大馋丫头实锤了 🔨" |
| **上周回顾** | 上周（周一至周日） | 每周一更新一次 | "上周总支出 842 元，比前一周少了 12%，精神生活倒是相当丰富 🎬" |
| **上月回顾** | 上个月（1 日至月末） | 每月 1 日更新一次 | "上个月你在购物上砸了 2150 元，都市丽人的衣柜永远少一件是吧 😏" |

### 无数据时的兜底

如果对应时间段没有任何消费记录，AI 生成温和的调侃而非报错：

> 昨天没有消费，不知道你是不是为了省钱又偷偷瞒着家人吃泡面了，对自己好点，一日三餐不能落下~

上周/上月同理，AI 自由发挥但遵循同一准则。

### 内容准则

1. **调侃消费行为，不攻击用户个人**
   - ✅ 餐饮占比高 → "大馋丫头/大馋小子实锤了"
   - ✅ 外卖点多 → "外卖战神，美团年度合伙人"
   - ✅ 娱乐消费高 → "精神生活相当丰富，主打一个及时行乐"
   - ❌ 严禁暗示用户该减肥、该省钱、该克制消费
   - ❌ 严禁评判用户生活方式或消费选择
   - ❌ 严禁给出任何形式的"建议"或"忠告"
2. **网络热梗自然融入**，参考抖音/小红书流行语，但不刻意堆砌
3. **性别由 AI 自行判断使用场景**：仅餐饮占比突出时可用"大馋小子/大馋丫头"，其他情况用中性称呼
4. **带免责** — 每条评论下方显示小字：

> *以上内容由腾讯混元 AI 生成，仅供娱乐参考，不构成任何建议。*

### 技术实现

调用方式：**前端直接调用**（`wx.cloud.extend.AI` 仅小程序端可用，云函数中不可用）。无需 API Key、无需配域名白名单、无需额外 SDK。

```javascript
// 前端 stats.js 或 utils/aiComment.js
const model = wx.cloud.extend.AI.createModel("cloudbase")
const res = await model.generateText({
  model: "hunyuan-lite",
  messages: [{ role: "user", content: prompt }]
})
// res.choices[0].message.content ← 俏皮评论
// res.usage.total_tokens ← Token 消耗
```

参考文档：https://docs.cloudbase.net/ai/model/miniprogram-access

### 频率控制

- 每条评论生成后缓存到本地 Storage，附带日期标记
- 昨日回顾：缓存键 `comment_daily_2026-05-17`，日期匹配则直接用缓存，不调 API
- 上周回顾：缓存键 `comment_weekly_2026-W20`，同一周不重复调
- 上月回顾：缓存键 `comment_monthly_2026-05`，同一月不重复调
- MVP 阶段先用通用模板兜底，正式上线后混元 AI 做主力

### Token 消耗估算

- 每次调用约 300-500 Token（prompt + 输出），取中值 400 Token
- 每个用户每月 3 次调用（每日/每周/每月各 1 次）≈ 1200 Token/月
- 100 个活跃用户 ≈ 12 万 Token/月，1 亿 Token ≈ 可持续使用约 83 个月（约 7 年）
- 用户不可能每天打开记 app，实际消耗更少

---

## 六、技术架构

### 整体方案

```
用户微信
  │
  ▼
小程序前端（WXML + WXSS + JS）
  │
  ▼
CloudBase SDK（wx.cloud）
  ├── 云数据库（文档型 NoSQL）
  ├── 云存储（账单照片）
  └── 云函数（复杂逻辑 / 微信登录换 openid）
```

**关键选型：**
- **不买服务器**，使用 CloudBase Serverless
- **不用 MySQL**，使用 CloudBase 文档数据库（类似 MongoDB）
- **不做传统登录**，使用微信一键授权
- **安全防护**：CloudBase 自带基础 DDoS 防护 + WAF + 微信封闭生态天然隔离 + 数据库权限仅创建者可读写 + 云函数输入校验 + 频率限制 + 敏感操作走云函数不暴露在前端

### 输入校验规则

每笔记账写入数据库前（云函数端），必须校验：

| 字段 | 规则 |
|:---|:---|
| type | 只能是 `"expense"` 或 `"income"` |
| amount | 数字，> 0 且 ≤ 99999999.99，小数点最多 2 位 |
| category | 字符串，长度 1-20 字，不能为空 |
| date | 格式 YYYY-MM-DD，不能是未来日期（允许今天） |
| note | 可选，长度 ≤ 200 字 |
| photoUrl | 可选，必须以 `cloud://` 开头 |

超出规则范围的直接拒绝，返回友好提示而非系统错误。

### 频率限制

- CloudBase 控制台 → 云函数 → 每个云函数设置 QPS 上限（建议初次设 50 QPS）
- 前端：同一用户 3 秒内只能提交一次记账（防手抖误触 + 防恶意刷）
- 数据库安全规则限制：单用户每天最多写入 500 条账单（远超正常使用量，防脚本灌数据）

### CloudBase 免费获取方式

1. **AI 小程序成长计划（首选）**：微信小程序后台 → 行业能力 → 报名，送 6 个月免费个人版环境 + 1 亿混元 Token（截止 2026-06-30）
2. **普通免费环境**：每个账号可创建 1 个免费环境，月送 3000 资源点
3. 开发期间完全免费，发布上线前无需付费

### 数据库设计（三个集合）

**bills（账单）**

```json
{
  "_id": "自动生成",
  "_openid": "用户 openid（微信自动获取）",
  "type": "expense",
  "amount": 36.5,
  "category": "餐饮",
  "date": "2026-05-09",
  "note": "午饭外卖",
  "photoUrl": "cloud://xxx.png",
  "createdAt": "2026-05-09T12:30:00Z"
}
```

**budgets（预算）**

```json
{
  "_id": "自动生成",
  "_openid": "用户 openid",
  "month": "2026-05",
  "amount": 2000,
  "createdAt": "2026-05-01T00:00:00Z"
}
```

**users（用户资料）**

```json
{
  "_id": "自动生成",
  "_openid": "用户 openid",
  "nickname": "微信昵称",
  "avatarUrl": "微信头像地址",
  "gender": "male",
  "customCategories": [
    { "name": "奶茶", "icon": "🧋" },
    { "name": "游戏", "icon": "🎮" },
    { "name": "宠物", "icon": "🐱" }
  ],
  "theme": "light",
  "budgetDefault": 2000,
  "createdAt": "首次登录时间",
  "lastLoginAt": "最近登录时间"
}
```
性别字段（`gender`）用于俏皮评论的个性化匹配。自定义分类（`customCategories`）每条包含名称和图标，用户新建分类时从预设图标池中自选。

---

## 七、开发实施顺序

```
Step 0: CloudBase 环境搭建（地基）
  ↓
Step 1: 微信授权登录（身份识别）
  ↓
Step 2: 记账页（数据唯一入口，心脏）
  ↓
Step 3: 首页 + 统计页（数据消费，可并行）
  ↓
Step 4: 预算页（依赖账单数据做对比）
  ↓
Step 5: 我的页面（设置与分类管理）
```

### Step 0 — CloudBase 环境搭建
- 去微信小程序后台 → 行业能力 → AI 小程序成长计划 → 获取环境 ID
- 在小程序项目 `app.js` 中：`wx.cloud.init({ env: "你的环境ID", traceUser: true })`
- 验证：写一行代码往数据库插入一条测试数据，能读出来就通了

### Step 1 — 微信授权登录
- `wx.login()` 拿临时 code → 云函数 `wxContext.OPENID` 换 openid
- 在 `users` 集合创建/更新用户记录
- 用户感受：打开小程序 → 点"允许" → 进入首页

### Step 2 — 记账页
- 收入/支出切换、金额输入、分类选择（含自创）、日期选择、备注输入、照片上传（ActionSheet 弹窗：拍照 / 从相册选择）
- 数据存入 `bills` 集合
- 照片存入云存储，URL 回写到 `photoUrl` 字段

### Step 3 — 首页 + 统计页
- 首页：从 `bills` 查本月汇总 + 最近 N 条
- 统计页：按分类聚合 → 饼图/柱状图 + 月度趋势 + 俏皮评论匹配

### Step 4 — 预算页
- 用户设定/修改月度预算 → 存入 `budgets` 集合
- 关联 `bills` 计算本月已花 → 进度条展示

### Step 5 — 我的页面 + 新手引导
- 头像/昵称展示、性别设置、自创分类管理、主题切换（本地 Storage）、数据删除
- 新手引导：3 页 swiper，`wx.setStorageSync('has_seen_guide')` 控制只显示一次

---

## 八、用户开发者画像（给 AI 的上下文）

- 微信小程序开发经验：上课学过，做过一个 IoT 温室监控小程序（手写 MQTT WebSocket 通信）
- 代码能力：**看得懂，但自己独立敲不出来**，架构设计需要辅助
- AI 使用经验：用过 Claude Code 辅助开发
- 云服务经验：**零**，从未碰过 CloudBase、云数据库、云函数
- 数据库经验：学过 SQL 但觉得"古老、安装麻烦"，对 NoSQL 不了解
- 专业：物联网工程（非软件工程/大数据）
- 目标：做一个**真正能上线、能用的**微信小程序，不只是交作业
- 预算偏好：**能白嫖就不花钱**

---

## 九、商业模式（后续版本考虑）

| 版本 | 定位 | 功能 |
|:---|:---|:---|
| MVP（当前） | 完全免费 | 基础记账、统计、预算、俏皮评论 |
| 高级版（未来） | 基础免费 + 增值付费 | 详细统计分析、多账本、预算模板、Excel 导出、数据同步 |

---

## 十、名词对照（给不熟悉的开发者）

| 名词 | 大白话解释 |
|:---|:---|
| CloudBase / 云开发 | 腾讯提供的后端服务，不用买服务器就能搞定数据库、文件存储和登录 |
| 云数据库（文档型 NoSQL） | 数据存成 JSON，读写像操作 JavaScript 对象，**不需要建表、不需要写 SQL** |
| 云存储 | 存文件的地方，比如用户拍的账单照片 |
| 云函数 | 跑在云端的 JavaScript 代码，处理登录、复杂计算等，不需要自己搭服务器 |
| _openid | 微信给每个用户在小程序里的唯一 ID，自动获取，用户无感 |
| Serverless | "没有服务器的后端"，你只管写代码，云平台帮你跑 |

---

## 十一、当前实现进度（2026-05-18 更新）

本节记录"代码已落地"vs"云端配置/能力未到位"的实际状态，避免后续协作误判功能完整度。

### 已实装并可用

- 用户登录、首页、记账、预算、月度环形统计（沿用上版）
- **账单 CSV 导出**：顶部 meta 头含用户身份元信息（昵称、性别、条数），3 选项后续操作（打开 / 分享 / 保存到手机）
- **"我的"页头像 / 昵称在线编辑**（经过多轮硬化）：
  - 头像链路：`wx.chooseMedia` → `wx.editImage`（裁剪 / 旋转，失败静默回退到原图）→ `wx.compressImage(quality:80)` → 云存储 → `users.avatarUrl` 落库 → `wx.cloud.getTempFileURL` 解析成 https 临时 URL 再渲染
  - 关键 lesson：`<image src="cloud://...">` 在部分基础库下静默渲染失败，必须显式拿 https URL；落库不再走通用 `updateUserField`（内部 swallow 错误），直接 `db.collection().where().update()` 并检查 `stats.updated === 0` 抛错
  - 昵称：弹窗 1-20 字校验，允许清空回退默认"橘记用户"
  - WXML：占位 emoji 🍊 + `binderror` 兜底，加载失败不空白

### 已写好代码但云端调用尚未跑通

- **统计页 AI 俏皮评论（本轮核心新增）**：
  - UI：三张卡片（📅 昨日回顾 / 📊 上周回顾 / 📈 上月回顾），Dusty Rose "小橘的便签"风格 —— 左侧 3rpx 玫瑰色竖线（`::before` 伪元素，顶 / 底内缩 16rpx 不贯穿）、emoji 衬底块三色阶（奶玫瑰 / 奶米粉 / 豆沙粉，与环形图配色同源）、双层 rose-tinted box-shadow（不是黑阴影）、文案 32rpx letter-spacing 0.5rpx line-height 1.75、虚线分隔免责声明、loading 时 2.4s opacity 呼吸动画
  - 数据：daily / weekly / monthly 并行加载、按 `periodKey` 自动失效缓存（昨日日期变 / 上周区间变 / 上月 monthKey 变）、失败走 fallback 不写缓存
  - AI 调用按 CloudBase 官方文档：`wx.cloud.extend.AI.createModel("cloudbase").streamText({ data: { model: "hy3-preview", messages: [...] } })`，`for await (event of res.eventStream)` 累加 `choices[0].delta.content`
  - **当前状态：CloudBase 控制台 `hy3-preview` 模型尚未接入 / 未开放小程序端调用**，因此 AI 调用未跑通；统计页三张卡片会显示 fallback `小橘暂时没词儿了，刷新试试 ✨`
  - **下一步行动**：CloudBase 云开发控制台 → AI+ 扩展能力 → 接入 hy3-preview 并开通小程序端权限 → 真机 / 工具进统计页查 Console `[ai]` / `[ai-{scope}]` 日志验证

### 工程改善（与上版相比）

- 错误处理：所有云端调用带可观察的 `console.log / warn` 标签（`[avatar]` / `[ai]` / `[ai-daily]` 等），失败时 toast 显示具体 errMsg 而非笼统"出错"
- 数据库写入：头像落库直接 `db.collection().where().update()` + 检查 `stats.updated === 0` 抛错，不再被通用工具静默 swallow
- UI 兜底：image `binderror` 回退、AI 失败不写缓存允许下次重试

### 已知遗留（沿袭未变）

- 自定义分类管理页未做（"我的"页点击仍是 toast "即将开放"）
- 引导页仍为传统 swiper，未升级到 Spotlight 高亮引导
- `cloudfunctions/bills` 服务端校验链路写好但记账页未接入（仍前端直写）
- `clearData()` 仅删 bills 不删 budgets，弹窗文案与行为不一致

---

## 十二、工程现状盘点（2026-05-21 · 终版）

> **设计审计 + 全量重构后**的真实状态记录。上一版 §11/§12 中的冗余与遗留项已被本轮 5 次迭代（525 行 → 375+350+175+375 行增量）系统性消化。

### 1. 10 页面完整清单（5 Tab + 登录 + 引导 + 3 子页面）

| 页面 | 路由 | 类型 | 核心能力 |
|:---|:---|:---|:---|
| **登录** | `pages/login/login` | 入口页 | 柔光渐变 + 8 漂浮 SVG drift 动画 + 微信一键登录 |
| **引导** | `pages/guide/guide` | 首次 | 4 页 swiper（柔光背景 + emoji 入场动画 + 自定义圆点指示器 + 第 4 页 logo + gradient CTA） |
| **首页** | `pages/home/home` | Tab | 今天/昨天双数字 + 预算三态进度条 + 季节标签 + 账单按日分组（近 30 条）→ 点击跳详情 / 长按删除 |
| **统计** | `pages/stats/stats` | Tab | 月份选择器 + 收/支 segment toggle + 甜甜圈类目图 + 6 月趋势柱状图 + AI 三卡片 |
| **记账** | `pages/record/record` | Tab | 收/支滑块 + 104rpx 金额 + 分类网格（含自创弹窗） + 日期 picker（今天/昨天智能显示） + 心情选择器（8 预设 emoji + 自定义） + 备注 + 圆形相机按钮 + 保存 |
| **预算** | `pages/budget/budget` | Tab | CSS 进度环（scale-in 动画） + 6 月达成率历史条形图 + bottom-sheet 调整面板 + 空态引导 |
| **我的** | `pages/profile/profile` | Tab | 头像/昵称硬化编辑 + 记账足迹卡（天数/笔数/最爱分类 → 点击生成 AI 海报） + AI 海报弹窗（loading/failure 态） + 性别 + 主题 + 分类管理入口 + CSV 导出 + 数据清除 |
| **分类管理** | `pages/categories/categories` | 子页 | 预设/自创列表 + 弹窗 CRUD + emoji 图标池（spread operator 修复） + `users.customCategories` 同步 |
| **主题预览** | `pages/themes/themes` | 子页 | 4 套色板卡片（清新/深夜/薄荷/暖阳） + 选中 checkmark + `wx.reLaunch` 全局刷新 + db 同步 |
| **账单详情** | `pages/detail/detail` | 子页 | 暖奶油渐变底 + 拍立得照片框 + 心情圆形印章 + 分类 icon + 日历日期 + 信纸横线纹理备注 + 删除 |

### 2. 主题系统（设计系统级实现）

- **工程实现**：`miniprogram/utils/theme.js` — `initAppTheme(app)` 在 `onLaunch` 时从 Storage 读 `theme` id 并注入 `page{}` 根级别 CSS 变量；`applyTheme()` 在每个 Tab 页 `onShow` 调用
- **4 套主题**：清新（Dusty Rose `#785655`）、深夜（深底暖金 `#d4a574`）、薄荷（浅绿翠绿 `#3d7a5c`）、暖阳（奶黄暖橙 `#d4865a`）
- **持久化**：Storage + `users.theme` 数据库同步（跨设备）
- **切换方式**：themes 预览页选择 → 确认弹窗 → `wx.reLaunch`

### 3. AI 模块（双引擎）

**文本生成**（stats 页「小橘的便签」）：
- `hunyuan-v3` → `hy3-preview`，`streamText` ↔ `textStream` / `eventStream` 双通道
- 串行调用（间隔 1s）+ 429 退避重试（3s / 6s）+ `periodKey` 缓存 + 失败不写缓存

**图像生成**（profile 页「AI 海报」）：
- 云函数 `aiPoster` + `generateImage-WtU3mJ`（CloudBase AI+ 扩展）
- 入口：足迹卡三项（天数 / 笔数 / 最爱分类）点击触发
- prompt 由足迹数据驱动（如「记录 365 天的消费足迹」）
- 每日额度限制 + loading 旋转动画 + 轮播提示文案 + 错误降级

### 4. 数据流

| 集合 | 字段 | 写入路径 | 云函数校验 |
|:---|:---|:---|:---|
| `bills` | `type, amount, category, date, note, photoUrl, mood, createdAt` | `record.js` 客户端 `db.add` | `cloudfunctions/bills` 已写未接入 |
| `budgets` | `month, amount, createdAt` | `budget.js` 客户端 `db.add` | 无 |
| `users` | `nickname, avatarUrl, gender, customCategories, theme` | `profile.js` / `app.js` | `quickstartFunctions` 初始化 |

### 5. 产品冗余清理结果

| 冗余项（§11/§12 识别） | 状态 |
|:---|:---|
| 首页 vs 统计同展「本月总支出」 | ✅ 已修复 — 首页改为今天/昨天，stats 保留月度 |
| 「记一笔」三入口 | ✅ 已修复 — 删除首页中央按钮，仅保留底部 Tab |
| 统计页收入维度缺位 | ✅ 已修复 — segment toggle 支出/收入切换 + 甜甜圈联动 |
| 账单列表无分组/不可点击 | ✅ 已修复 — 按日分组 + 点击跳详情页（手账风格） |
| AI「昨日回顾」错位 | ❌ 保留（产品创新点，stats 页三张卡片完整保留） |

### 6. 已知遗留

- `cloudfunctions/bills` 服务端校验已写但未接入（仍前端直写 `bills`）
- `clearData()` 仅删 `bills` 不删 `budgets`
- 新手引导未升级到 Spotlight 高亮引导
- AI 图标生成（混元文生图 → 自创分类图标）未实现

---

> **最后更新：** 2026-05-21
> **下一步：** CloudBase 控制台接入 `hy3-preview` → 跑通统计页 AI 评论 → 推进自定义分类管理页
