# 橘记JUJI小程序 · 上线前冒烟测试大纲

> 版本：v2.0（2026-09-14 重构） · 基线：main @ 当前工作区（含 profile.js 上传修复、privacy 复制修复、users fileID 截断修复）
> 方式：微信开发者工具自动化接口（miniprogram-automator，端口 9420）+ 统一脚手架 `test/e2e/lib/harness.js`
> 真实云环境：`lajiaoyou-d4g78yts61f1a841d`

---

## 〇、为什么是 v2.0（重构动机，必读）

2026-09-14 连续暴露两个"测试全绿却线上报错"的事故，根因都是**测试设计缺陷**，本大纲据此重写：

| 事故 | 现象 | 根因（测试侧） | 本版修复 |
|------|------|---------------|----------|
| ① 上传头像成功却弹乱码 / 我的页"未设置" | 用户看得见，测试 14/14 PASS | 断言太弱——A3 三页只断言 `themeStyle` 存在即 PASS；**导入函数被误加 `this.` 抛 TypeError 被 catch 吞掉**，控制台有报错但测试没接 | 强断言 + **控制台 error 即失败** + 断言计数 |
| ② users 云函数 fileID 被截断到 60 字，归属校验永远失败（换头像永远失败） | 纯服务端 bug，UI 点不出来 | 无云函数契约测试；`users` 4 个 action **一个都没测** | 新增 `smoke-cloudfn.js` 双向契约套件 |

**结论**：旧的"全绿"是假象。本版把"绿得有意义"写进脚手架硬规则（见 §一）。

---

## 一、三条硬规则（脚手架强制，不可绕过）

`test/e2e/lib/harness.js` 在每次 `h.expect()` 中强制：

1. **控制台零报错**：用例执行期间小程序控制台出现 `console.error` → 该用例**直接判失败**。
   （事故①的乱码弹窗本质是 `uploadAvatar` 抛 `TypeError`，控制台有完整报错，旧测试一句没接。）
2. **禁止「元素存在即通过」**：断言必须落在**业务数据**上（值、数量、格式、一致性），不得只断言 `element !== null` 或 `themeStyle` 存在。
3. **断言计数**：每条用例统计断言数，为 0 的用例在报告里标记 ⚠，防止空用例混成 PASS。

> 报告固定输出：用例数 / 通过数 / **断言总数** / **控制台 error 条数**。三者齐全才算"绿得有意义"。

---

## 二、测试架构（双主线：审核关切点 + 代码改动回归）

| 套件 | 文件 | 主线 | 覆盖 | 用例数 |
|------|------|------|------|--------|
| **P0 功能回归** | `test/e2e/smoke-p0.js` | 代码改动回归 | 记账主链路（记→见→编辑→删除）+ 四页业务数据强断言 | 14 |
| **审核关切点** | `test/e2e/smoke-review.js` | 审核关切点 | 隐私链路 + 静态合规基线 + 关键页可用性 + AI 合规 | 24 |
| **云函数契约** | `test/e2e/smoke-cloudfn.js` | 代码改动回归（服务端） | 10 个云函数 action 双向契约（合法→success / 非法→拒绝） | 28 |
| **覆盖矩阵** | `test/coverage-matrix.js` | 元测试 | 静态交叉比对页面/action 与三套件的覆盖缺口 | — |
| **脚手架** | `test/e2e/lib/harness.js` | 公共 | 连接 / 控制台监听 / 强断言 / 报告 / 清理 | — |

**运行**（先强制重编译加载磁盘改动）：
```bash
cd test/e2e && node start-auto.js --force   # 启动开发者工具自动化（端口 9420）
node smoke-p0.js        # 功能回归
node smoke-review.js    # 审核关切点
node smoke-cloudfn.js   # 云函数契约
cd test && node coverage-matrix.js   # 覆盖缺口核查
```
三套件全绿 + 覆盖矩阵 0 未知缺口 ⇒ 可提交审核。

---

## 三、放行门槛（"测完即可过审"的定义）

下列**全部满足**才允许提交微信审核平台：

- [ ] `smoke-p0`：14/14 PASS，控制台 error = 0
- [ ] `smoke-review`：24/24 PASS，控制台 error = 0（**审核关切点零缺口**）
- [ ] `smoke-cloudfn`：28/28 PASS，控制台 error = 0（**云函数契约零回归**）
- [ ] `coverage-matrix`：退出码 0（无未知缺口）
- [ ] 人工核对项（见 §七，自动化无法覆盖）：类目资质、隐私指引与实际行为一致性、真机授权弹窗

> 任一自动化套件 FAIL ⇒ **终止提交，转修复**。不得用"历史全绿"当安全感（见 §〇）。

---

## 四、审核关切点覆盖矩阵

依据 `docs/隐私合规/03-提审与审核跟进手册.md`（§1.2 代码侧自查 11 项 / §1.3 真机清单 10 项 / §三 高频驳回话术 10 条）。

| 审核员会看什么 | 对应用例 | 断言要点 |
|----------------|----------|----------|
| 隐私协议入口可达、全文可读 | B1 / B2 / B3 / B4 | 登录页两协议链接齐全；隐私页段落 ≥15 含"收集/删除/撤回/第三方"；可切用户协议 Tab |
| 拒绝授权不崩、不强制 | A4 / A3 | 授权按钮 `open-type="agreePrivacyAuthorization"`；敏感 API 前有 `requirePrivacyAuthorization` |
| 未声明接口不调用（剪切板未声明） | A7 / B4 | 代码不依赖 `setClipboardData`；邮箱改 `user-select` 长按复制 |
| 无诱导分享 / 无强制授权 / 无旧版用户信息接口 | A8 / A9 | 分享文案无"红包/助力"等；不用 `getUserProfile/getUserInfo` |
| 关键页不得白屏 / "未设置" / 报错 | C1 / C2 / C3 / C4 / C5 | 昵称≠默认、头像可渲染、版本号==`VERSION`、统计/预算/帮助数据完整 |
| AI 免责声明 / 违规拦截 / 不编造实时数据 | D1 / D2 / D3 | 弹窗含"AI生成仅供参考"；违规输入返回兜底不吐模型输出；天气不编造气温 |
| 隐私开关开启 | A1 | `app.json` `__usePrivacyCheck__: true` |
| 调试旁路关闭 | A2 | `PRIVACY_DEBUG_BYPASS === false` |
| 云函数 openapi 权限声明 | A5 | `contentSafety/users/aiChat` 声明 `security.msgSecCheck` |
| 无硬编码密钥 | A6 | 全仓扫描无 AppSecret/私钥 |
| 清除数据有二次确认（不误触清空） | A10 | `clearData` 有 `showModal` 二次确认；任何套件都不调用 `clearUserData` |
| 我的页数据与隐私入口齐全 | B5 / B6 | 导出/导入/隐私协议/用户协议/清除入口均在 |

---

## 五、代码改动 / 历史 bug 回归矩阵

| 修复点（来自工作记忆与本次事故） | 回归用例 | 防的是什么 |
|--------------------------------|----------|------------|
| 导入函数误加 `this.`（`this.resolveAvatarSrc` 抛 TypeError） | C1（昵称/性别/头像强断言）、U6/U7/U8、B1（登录）→ 控制台 error 即失败 | 静默崩溃被 catch 吞掉、页面渲染半截 |
| users 头像 fileID 被截断到 60 字 → 归属校验失败 | U8（构造 >90 字合法 fileID 必须被接受并还原） | 换头像永远失败的回归 |
| users 头像归属校验（只能改自己的 `avatars/<openid>_`） | U7（他人文件→拒绝）、U6（非 cloud://→拒绝） | 越权引用他人文件 |
| 隐私页"复制邮箱"原先是坏的（`setClipboardData` 未声明作用域） | B4（改 `user-select` 长按复制）、A7 | 点了复制却毫无反应 |
| 记账主链路（记→见→编辑→删除） | A1-1/A1-2/A1-3/A1-4/A2-1/A2-2/A2-3 | 资金链路端到端可用 |
| 金额校验（0/负/超长/三位小数） | A1-4（脏数据 0 入库）、B3（三位小数拒绝） | 脏数据入库 |
| 软删除 `isDeleted` | A2-3（查库 `isDeleted=true` 生效） | 删了还在 |
| 预算/统计/首页业务数据 | C2/C3/C4（强断言，非"元素存在"） | "未设置"占位蒙混过关 |
| 主题幂等（写回原值不污染） | U2/U4（读基线→写回原值） | 测试改坏真实主题 |

---

## 六、云函数契约覆盖（来自 coverage-matrix）

10 个云函数，除「已知可接受缺口」外全部双向覆盖：

- **users**：`updateProfile`(U4/U5) · `updateAvatar`(U6/U7/U8) · `updateTheme`(U2/U3) · `updateCustomCategories`（已知缺口，见 §七）
- **bills**：`create`(B1/B2/B3) · `delete`(B1/B4) · `update`/`batchCreate`（已知缺口，见 §七）
- **budgets**：`upsert`(G1/G2/G3/G4)
- **contentSafety**：空内容放行(S1) · 违规拦截(S2) · 正常文案(S3)
- **aiPoster**：`profileTitle`(P1) · 写信(P2，结构断言含兜底)
- **quickstartFunctions**：`getOpenId`(Q1) · `syncUser`(Q2)
- **dataMigration**：`export`(M3) · `import`(M2) · 未知 action(M1)
- **exportBills**：(E1，形状断言)
- **clearUserData**：**绝不调用**，仅静态守卫(A10)
- **aiChat**：跨套件由 `smoke-review` D2/D3 覆盖（违规拦截 + 天气编造）

---

## 七、已知缺口与 allowlist（诚实标注，非盲区）

覆盖矩阵 `coverage-matrix.js` 会把这些登记为「🟡 已知缺口」，不拉低放行门槛：

| 缺口 | 为何可接受 |
|------|------------|
| 页面 `guide` / `categories` / `themes` 无专用用例 | 三者底层逻辑已被覆盖（引导标记由 syncUser、分类由 users 契约、主题由 updateTheme 契约）；属低优先级 UI 页，后续补充渲染级用例即可 |
| `users.updateCustomCategories` 未直接断言 | 依赖 msgSecCheck 正向路径，openapi 权限波动会误判；由内容安全(S2) + 审核套件兜底 |
| `bills.update` / `bills.batchCreate` 未直接覆盖 | `create`/`delete` 已覆盖，二者校验与 create 同源（`validate()` 共用），回归风险低 |

**`coverage-matrix.js` 退出码规则**：仅当存在**未登记**的缺口（如将来新增云函数 action 忘了测）才返回 1 —— 这是防回归的"看门狗"。

---

## 八、数据安全与清理策略（重要）

测试写入**真实云数据库**（个人版，creator-only，即开发者本人数据），必须隔离并清理：

1. `smoke-p0` 所有测试账单备注统一带 `[SMOKE]` 标记，金额用不易混淆值（6.66 / 9.99）；结束前按标记清库并断言**残留 0 条**。
2. `smoke-cloudfn` 写类用例采用「**读基线 → 写回原值**」幂等往返：主题/性别/头像均还原，不污染真实资料；合成月份 `2099-01/02` 仅用于 budgets 往返。
3. `smoke-review` 全程只读，**不写任何数据**。
4. `clearUserData` 云函数**任何套件都不调用**（会清空真实数据），仅 `smoke-review` A10 做静态代码走查（确认有二次确认 + 无套件调用它）。

---

## 九、执行顺序与命令

```
start-auto.js --force        # 强编加载磁盘改动（必须）
  → smoke-p0.js              # 功能回归（先跑，最先暴露主链路问题）
  → smoke-review.js          # 审核关切点
  → smoke-cloudfn.js         # 云函数契约
test/coverage-matrix.js       # 覆盖缺口核查（退出码 0 方可提交）
```

产物：`test/e2e/artifacts/<日期>/{p0,review,cloudfn}-report.md` + `coverage-matrix.md` + 各页截图。

---

## 十、风险与预案

| 风险 | 预案 |
|------|------|
| 真实写入污染个人数据库 | §八 标记+清理+复核三重保障 |
| AI 接口限流(429)/烧额度 | `smoke-review` D 组与 `smoke-cloudfn` P 组对 AI 调用做**结构/兜底断言**，不依赖文案质量；每日限额内最多真实调 1–2 次 |
| AI 回复不确定导致断言不稳定 | 只断言"非兜底 + 结构合法"，不比对原文 |
| 云函数冷启动慢 | 调用超时 60s，步骤间轮询而非固定 sleep |
| **测试"假绿"**（历史教训） | 脚手架三条硬规则（§一）：控制台 error 即失败、业务数据强断言、断言计数 |
| 改了代码没重编译 | `start-auto.js --force` 强制 close+open 重新编译；否则 automator 跑的是旧产物 |
| 多 Agent/工具并行改同一仓库 | 提交前 `git fetch` + `git ls-remote` 确认远端；不明提交先 `git show --format=%an` |

---

## 十一、待补充（低优先级，不阻塞提交）

- `guide` / `categories` / `themes` 三页渲染级用例（补齐后 coverage-matrix 页面覆盖达 100%）。
- `bills.update` / `bills.batchCreate` 契约用例（构造合法 billId 做 update 往返）。
