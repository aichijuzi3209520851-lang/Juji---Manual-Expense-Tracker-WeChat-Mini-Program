# 橘记小程序 · 上线前冒烟测试大纲

> 版本：v1.1（2026-09-13） · 基线：main @ f80d595 + 工作区未提交改动（折线图修复 / TabBar 毛玻璃 / 隐私勾选重设计）
> 方式：微信开发者工具自动化接口（miniprogram-automator）全量 UI 冒烟 + 云数据库断言

---

## 一、测试目标与判定标准

验证小程序核心链路在真实云环境（lajiaoyou-d4g78yts61f1a841d）下端到端可用，为提交审核提供放行依据。

| 级别 | 定义 | 放行标准 |
|------|------|----------|
| P0 | 核心资金链路：记账/改账/删账、登录、统计渲染、预算 | **100% 通过** |
| P1 | 重要辅助：分类管理、主题、导出、TabBar、页面元素完整性 | ≥ 90% 通过，缺陷须有绕行方案 |
| P2 | 体验与边缘：动画、AI 真实调用、窄屏适配 | 记录不阻塞 |

**阻塞级缺陷定义**：无法登录 / 无法记账 / 统计页白屏或数据错位 / 云函数 5xx / 主题切换后界面残留他色。出现任一即终止测试转修复。

## 二、测试范围

**In scope**：10 个页面 + 自定义 TabBar + 11 个云函数中 10 个在用（`generateImage-WtU3mJ` 为废弃占位，不测）+ 主题系统（4 预设 + 自建 + 深色）+ **小橘 AI 全链路**（记账页助手聊天/一句话记账/建议话术、统计页便签三卡、今日称号、小橘心里话、AI 信件、双端内容安全）+ 本轮 3 处未提交改动的回归（折线图几何/毛玻璃/隐私勾选行）。

**Out of scope**（自动化无法覆盖，标注转真机人工）：
- 真实相机拍摄、微信会话分享面板、手机振动、订阅消息
- 真实 AI 大模型质量（仅验证链路与兜底，不评估文案质量）
- 审核合规类人工项（类目资质、隐私指引与实际行为一致性人工复核）

## 三、环境与前置条件

| 项 | 值 / 要求 |
|----|-----------|
| 开发者工具 | 已开启服务端口 59327 + 自动化默认信任项目（✅ 已具备，见截图） |
| 自动化驱动 | Node ≥ 14 + `miniprogram-automator`（安装于 `test/e2e/`，独立 package.json，不污染根目录） |
| 连接方式 | `automator.launch({ cliPath: <工具cli>, projectPath, port: 9420 })`，失败则回退 `automator.connect` |
| 登录态 | 依赖工具当前登录票据（已开启"允许获取工具登录票据"），openid 为开发者本人 |
| 基线主题 | 测试开始前记录当前主题 ID，结束后复原 |
| 产物目录 | `test/e2e/artifacts/<日期>/`：每用例截图 + `report.md` + 缺陷清单 |

## 四、数据安全与清理策略（重要）

测试会写入**真实云数据库**（个人版，creator-only，即开发者本人数据），必须隔离并清理：

1. 所有测试账单备注统一带标记 `[SMOKE]`，金额使用不易混淆值（如 6.66 / 8.88）；
2. 测试预算使用当月金额精确值（如 66.66），结束后改回原值（开始前先读取并记录原预算）；
3. 清理阶段：按 `[SMOKE]` 标记查询并删除测试账单（走 `bills` 云函数软删除 + 记录 id 清单）、删除测试分类/主题、复原预算与主题存储键；
4. **小橘一句话记账的特殊清理**：AI 提取的账单备注取自用户输入，不保证带标记——该用例确认入库后**用例内立即删除**；统一清理阶段再按"金额 6.66 + 日期特征"兜底查删一次；
5. 清理后用首页流水数量 + 统计总额复核复原成功；
6. `clearUserData` 云函数**绝不**在冒烟中调用（会清空真实数据），仅静态代码走查。

## 五、测试用例大纲

### M0 连接与基础（P0，1 用例）
| 编号 | 用例 | 步骤/断言 | 方式 |
|------|------|-----------|------|
| C0-1 | 自动化链路连通 | launch 成功；`systemInfo` SDKVersion ≥ 3.16.0；pageStack 可获取；对项目截图 1 张 | automator |

### M1 登录与隐私（P0，5 用例）— 含本轮回归
| 编号 | 用例 | 步骤/断言 | 方式 |
|------|------|-----------|------|
| C1-1 | 登录页渲染 | logo/标题"橘记"/副标题/登录按钮/协议行元素齐全，截图 | UI |
| C1-2 | 未勾选禁用登录 | `privacyAgreed=false` 时按钮 `disabled=true` | UI+data |
| C1-3 | 勾选交互回归 | 点击协议行 → `privacyAgreed=true`、勾选框 `is-checked`、按钮可点；再点取消 | UI+data |
| C1-4 | 一键登录链路 | 勾选后触发按钮 → `agreePrivacyAuthorization` 回调 → silentLogin → 跳转首页（storage 写入授权记录） | UI+evaluate |
| C1-5 | 协议可打开 | 分别点《隐私协议》《用户协议》→ 正常打开且可返回 | UI |

### M2 新手引导（P2，1 用例）
| C2-1 | 引导 4 页 swiper | evaluate 清 `has_seen_guide` 并 reLaunch → swiper 4 页可滑、完成回首页、标记回写 | UI+evaluate |

### M3 记账（P0，6 用例）— 数据源，最先执行
| 编号 | 用例 | 步骤/断言 | 方式 |
|------|------|-----------|------|
| C3-1 | 分类宫格渲染 | 预设分类数量、图标、文字齐全 | UI |
| C3-2 | 记支出（主链路） | 输入 6.66 + 选"餐饮" + 备注 `[SMOKE]支出` → 保存 → 云函数 bills/create 成功，云库可查到该笔 | UI+evaluate |
| C3-3 | 记收入 | 切收入 → 8.88 `[SMOKE]收入` → 保存成功 | UI+evaluate |
| C3-4 | 金额校验拦截 | 空金额/0/负数/超 8 位/两位小数以上 → 均被拦截且有提示，无脏数据入库 | UI+evaluate |
| C3-5 | 心情与照片 | 选心情标签；`mockWxMethod('chooseMedia')` 注入测试图 → 真实 `uploadFile` 到云存储 → photoUrl 以 `cloud://` 开头，测后删文件 | mock+evaluate |
| C3-6 | 防抖与频控 | 连续快速点保存仅入 1 笔（3s 防抖） | UI+evaluate |

### M4 首页（P0，3 用例）
| 编号 | 用例 | 步骤/断言 | 方式 |
|------|------|-----------|------|
| C4-1 | 首页渲染 | 今日/昨日卡、预算条、分组流水结构完整，截图 | UI |
| C4-2 | 数据联动 | C3-2 记账后首页今日支出 +6.66，`[SMOKE]` 流水可见 | UI+data |
| C4-3 | 点流水进详情 | 点击 `[SMOKE]` 流水 → navigateTo 详情页且 id 正确 | UI |

### M5 统计（P0，6 用例）— 含折线图回归
| 编号 | 用例 | 步骤/断言 | 方式 |
|------|------|-----------|------|
| C5-1 | 三档周期切换 | 天/周/月 tab 切换，标题与副标随动，无报错 | UI |
| C5-2 | 折线图渲染回归 | 月视图 canvas 截图非空白；横轴 12 个刻度全部可见（修复点）；刻度 x 与数据点 x 逐位一致（evaluate 读取 tick 数据） | 截图+data |
| C5-3 | 触摸选点回归 | 模拟 touch 点击折线中部 → tooltip 出现、选中月份正确（坐标换算修复点） | UI+data |
| C5-4 | 支出/收入切换 | 切收入后总额/折线随数据变化 | UI+data |
| C5-5 | 排行榜 | Top3 展示、展开全部/收起交互 | UI |
| C5-6 | AI 评论链路 | 默认命中缓存或 loading→文案渲染；用 mock 注入一次失败 → 兜底文案"小橘暂时没词儿了"出现（不真实调模型，防限流/烧额度） | mock |

### M6 预算（P0，3 用例）
| 编号 | 用例 | 步骤/断言 | 方式 |
|------|------|-----------|------|
| C6-1 | 设当月预算 | 先记录原预算 → 设 66.66 → budgets 集合写入/更新成功 | UI+evaluate |
| C6-2 | 进度环与联动 | 进度环按 6.66/66.66 渲染；首页预算条同步 | UI+data |
| C6-3 | 历史预算 | 近 6 个月历史列表渲染 | UI |

### M7 账单详情（P0，3 用例）
| 编号 | 用例 | 步骤/断言 | 方式 |
|------|------|-----------|------|
| C7-1 | 详情一致性 | 详情字段与创建值一致（金额/分类/日期/备注/心情） | UI |
| C7-2 | 编辑账单 | 金额改 9.99 → 保存 → 列表与云库同步为 9.99 | UI+evaluate |
| C7-3 | 删除账单 | 删除 → 二次确认 → 云库 `isDeleted=true`（软删除）→ 列表消失 | UI+evaluate |

### M8 分类管理（P1，3 用例）
| 编号 | 用例 | 步骤/断言 | 方式 |
|------|------|-----------|------|
| C8-1 | 列表渲染 | 预设+自定义分类完整渲染 | UI |
| C8-2 | 新增自定义 | emoji + 名称 `[SMOKE]类` → users 云函数写 customCategories → record 页宫格同步出现 | UI+evaluate |
| C8-3 | 删除自定义 | 长按 → 二次确认 → 删除 → record 页同步消失 | UI |

### M9 主题系统（P1，3 用例）
| 编号 | 用例 | 步骤/断言 | 方式 |
|------|------|-----------|------|
| C9-1 | 预设主题切换 | 逐一切 4 套 → `themeChanged` 广播、页面 `themeStyle` 刷新、hero 色随动，各截 1 图 | UI+截图 |
| C9-2 | 深色模式回归 | 切 dark → TabBar 玻璃底为深色（computed style 断言 background alpha≈0.3），文字光晕生效 | UI+computedStyle |
| C9-3 | 自建主题 CRUD | 取色创建 → 应用 → 删除，列表同步 | UI |

### M10 我的（P1，4 用例）
| 编号 | 用例 | 步骤/断言 | 方式 |
|------|------|-----------|------|
| C10-1 | 页面渲染 | 头像/昵称/足迹热力图/功能入口齐全，截图 | UI |
| C10-2 | AI 信件 | 触发生成 → loading → 有信件文案或兜底（每日 30 限内最多真实调 1 次，P2 抽样） | UI |
| C10-3 | CSV 导出 | 触发 exportBills 云函数 → 返回 fileID 成功（不实际拉起分享面板） | evaluate |
| C10-4 | JSON 导出/导入 | dataMigration export 返回数据量 = bills 总数；import 走 mock 或仅验证入口 | evaluate |

### M11 TabBar（P0，2 用例）— 含毛玻璃回归
| 编号 | 用例 | 步骤/断言 | 方式 |
|------|------|-----------|------|
| C11-1 | 五 Tab 切换 | 逐个 switchTab，页面正确、高亮项正确（含中间"+"按钮） | UI |
| C11-2 | 毛玻璃回归 | computed style 断言外壳 `background` 为 `--color-surface-glass`（alpha≈0.35）且存在 `backdrop-filter`，截图 | computedStyle+截图 |

### M12 小橘 AI 专项（13 用例）

> 真实模型调用仅限标注 ★ 的 5 处（C12-2 / C12-3 / C12-11 / C12-12 / C10-2），串行执行且间隔 ≥1s，沿用 429 退避；其余全部 mock，防止限流与烧额度。

**A. 记账页小橘助手（与 M3 同页顺跑）**

| 编号 | 用例 | 步骤/断言 | 方式 |
|------|------|-----------|------|
| C12-1 | 宠物与聊天入口 | 小橘形象渲染、idle/随机动作类切换（petAction 25% 概率动作）；打开聊天弹窗 → 欢迎语"嗨，我是小橘…"渲染、建议话术 6 条齐全 | UI+data |
| C12-2 ★ | 建议话术查账（真实调用） | 点"今天记了几笔" → 回复渲染且非兜底文案；回复内容与云库当日 `[SMOKE]` 笔数可对账（验证 userProfile/categories/today 上下文传参） | UI+evaluate |
| C12-3 ★ | 一句话记账（真实调用，P0 资金链路） | 发"记一笔：[SMOKE]测试午饭 昨天花了6.66元" → 回复 + 草稿卡片（type=expense、amount=6.66、date=昨天、分类合法）→ 确认保存 → 云库新增该笔 → **用例内立即删除** | UI+evaluate |
| C12-4 | 多笔拆分与上限 | mock 云函数返回 2 笔 → 草稿卡片 2 条；mock 11 笔 → 截断为 10 条 | mock+UI |
| C12-5 | 输入内容安全拦截（mock） | mock `checkText` 失败 → 直接兜底文案"小橘不知道，来聊聊别的吧~"，断言未发起云函数调用 | mock+UI |
| C12-6 | 回复内容安全审查（mock） | mock 云函数返回敏感文案 → scene4 复查拦截 → 显示兜底 | mock+UI |
| C12-7 | 天气话题下线话术 | 发"今天天气怎么样" → 固定回复"天气查询功能先下线了…"，不调云函数 | UI |
| C12-8 | 失败兜底与超时 | mock callFunction reject → 兜底文案；静态断言客户端 timeout=65000（略大于云函数 60s） | mock+静态 |
| C12-9 | 键盘适配回归 | 输入框聚焦 → `chatKeyboardHeight>0`、自定义 TabBar 隐藏；失焦恢复；点击遮罩不误关弹窗 | UI+data |
| C12-10 | 消息渲染细节 | 长回复代码块样式（looksLikeCode→isCode）、用户头像（chatUserAvatarUrl）、新消息自动滚动定位 | UI |

**B. 今日称号与心里话（我的页，与 M10 顺跑）**

| 编号 | 用例 | 步骤/断言 | 方式 |
|------|------|-----------|------|
| C12-11 ★ | 今日称号（真实调用 1 次） | 当天有 `[SMOKE]` 支出 → 称号区渲染（AI 文案或规则兜底）；点击称号 → 解释弹窗内容非空、可关闭 | UI |
| C12-12 ★ | 小橘心里话（真实调用 1 次） | 触发入口 → loading"小橘想对你说说心里话" → 弹窗渲染内容或兜底文案，二次进入走缓存 | UI |
| C12-13 | 日限配置走查 | 静态断言 `aiChat` DAILY_LIMIT=0（当前不限）；记录回归说明：若改成正数，超限应回复"小橘今天聊得有点多了，明天再来吧~" | 静态 |

## 六、执行顺序（数据依赖链）

```
M0 连接 → M1 登录 → (M2 引导) → M3 记账 → M12-A 小橘助手(同页顺跑)
→ M4 首页 → M5 统计 → M6 预算 → M7 详情(改/删) → M8 分类 → M9 主题
→ M10 我的(含 M12-B 称号/心里话) → M11 TabBar → 清理复原 → 复核
```

★ 真实 AI 调用（共 5 处）在到达时串行执行；清理固定为最后一步；任何用例失败不中断整体，继续跑完收集全部缺陷，P0 失败除外（立即报告）。

## 七、产出物

1. `test/e2e/`：自动化脚本（按模块拆文件）+ `artifacts/<日期>/report.md`（每用例通过/失败/截图引用/耗时）；
2. 缺陷清单：编号 / 级别 / 复现步骤 / 截图 / 初步定位（文件:行号）；
3. 测试后云库核对清单：`[SMOKE]` 数据 0 残留、预算/主题/引导标记复原对照表。

## 八、风险与预案

| 风险 | 预案 |
|------|------|
| 真实写入污染个人数据库 | 第四节标记+清理+复核三重保障；执行前自动备份原预算/主题配置到本地 |
| AI 接口限流(429)/烧 Token | 除 ★ 标注 5 处外全部 mock；★ 用例串行 + ≥1s 间隔 + 沿用 429 退避重试 |
| AI 回复不确定导致断言不稳定 | 查账类只断言"非兜底文案 + 数字与云库对账"；记账类断言结构化草稿字段而非文案原文 |
| 云函数冷启动慢 | 全局超时 30s（AI 聊天用例放宽至 70s），每步之间轮询而非固定 sleep |
| automator 对原生组件(如键盘)受限 | 金额输入用 `element.input()`，弹层用 `page.callMethod` 直调（如直接调 `sendChatMessage`） |
| 工具登录态失效导致登录链路失败 | C1-4 失败时提示重新扫码，不伪造票据 |
